import { FormS, HeroTitle } from './Hero'

const Hero = () => (
  <FormS>
    <div className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </FormS>
)

export default Hero
